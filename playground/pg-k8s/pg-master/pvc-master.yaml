apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-master
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: postgres-sc-master